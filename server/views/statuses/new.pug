extends ../layouts/application.pug

block header
  = t('views.statuses.new.create')

block content
  +formFor()(id="status-create-form" action=route('createStatus') method='post')
    +input(status, 'name', errors)(type="text")
    +submit(status)(value=t('views.statuses.new.submit'))


